"""Project adapters: rest/graphql/browser/database/frontend/docker/dotnet/node/python/blockchain.

`rest`, `database`, and `frontend` are implemented. `frontend` covers
discovery + testability assessment only (no browser/UI execution) - see
`frontend/adapter.py` and `docs/FRONTEND_ANALYSIS.md`. The rest are reserved
for future phases (skill.md §14-15, §23). The adapter contract
(detect/describe/discover/generate_tests/execute/collect_metrics) is
documented in ARCHITECTURE.md §7.
"""
