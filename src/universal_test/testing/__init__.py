"""Test-type coordination: functional/performance/regression/reliability.

`performance/` is implemented as of Phase 4. `functional` testing lives in
`adapters/rest/` today (REST is still the only adapter); `regression` and
`reliability` are reserved for later phases (skill.md §8, §10-11, §23).
"""
