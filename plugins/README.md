# plugins/

Third-party/out-of-tree adapters that extend the framework without modifying
Core (`skill.md` §14). Empty until the plugin discovery mechanism is designed
alongside the built-in adapters (Phase 3+).
