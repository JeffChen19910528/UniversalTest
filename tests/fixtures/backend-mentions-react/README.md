# Backend Mentions React Fixture

This is a pure Python/FastAPI backend. Historically the team considered
building the dashboard in React or Vue, and this README mentions both, but
neither was ever added to the repository. Frontend detection must not treat
this prose mention as evidence of an actual React or Vue frontend.
