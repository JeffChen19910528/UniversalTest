{
  "name": "sveltekit-app-fixture",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "svelte": "^4.2.0"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.5.0",
    "vite": "^5.1.0",
    "vitest": "^1.2.0"
  }
}
