# schemas/

JSON Schemas for `universal-test.yaml`, the normalized test-spec format
(`skill.md` §16), and `report.json` (`skill.md` §19). Added alongside the
config loader hardening and the Phase 5 report generators.
